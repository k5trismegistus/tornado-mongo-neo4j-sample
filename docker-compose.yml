version: '3'

services:
  mongo:
    image: mongo:3.6
  neo4j:
    image: neo4j:3.3
  app:
    build: .
    command: python3 manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"
    depends_on:
      - mongo
      - neo4j
